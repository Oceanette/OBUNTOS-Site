const content = {
  pt: {
    title: "OBUNTOS — Arquivo Pessoal",
    description: "OBUNTOS — seu arquivo pessoal para Windows. Diário, tarefas, calendário, metas, pessoas, biblioteca, finanças e estatísticas em um só lugar.",
    skip: "Ir para o conteúdo",
    brandAria: "OBUNTOS, início",
    brandSubtitle: "arquivo pessoal",
    navAria: "Navegação principal",
    nav: ["Conheça", "O que tem", "Seus dados", "Baixar"],
    headerDownload: "Baixar para Windows",
    menuOpen: "Abrir menu",
    menuClose: "Fechar menu",
    switchAria: "Mudar site para inglês",
    switchTitle: "English",
    bannerLeft: "arquivo pessoal",
    bannerRight: "feito para Windows",
    bannerAlt: "Laboratório estilizado que faz parte da identidade visual do OBUNTOS",
    bannerNote: "Um espaço seu, dentro do seu computador.",
    signAria: "O OBUNTOS, arquivo pessoal",
    signSubtitle: "arquivo pessoal",
    heroEyebrow: "Um lugar para as coisas que fazem parte da sua vida",
    heroTitle: "Tudo o que importa para você, num lugar só.",
    heroLead: "O OBUNTOS junta anotações, tarefas, datas, pessoas, metas, arquivos e finanças sem fazer a sua vida parecer uma planilha. É um arquivo pessoal para abrir, organizar e voltar quando quiser.",
    heroPrimary: "Baixar para Windows",
    heroSecondary: "Ver o que tem dentro",
    releaseAria: "Informações da versão",
    release: ["<b>0.9.3</b> versão atual", "<b>Windows x64</b>", "<b>17,3 MB</b> setup"],
    storyEyebrow: "Feito para acompanhar você",
    storyTitle: "Seu dia muda. Seu arquivo vai junto.",
    storyText: "Tem coisa que merece ficar guardada: uma ideia, uma data, uma pessoa, um objetivo, um gasto, uma foto ou só um pensamento de hoje. O OBUNTOS coloca tudo perto sem misturar tudo.",
    storyCards: [
      ["Anote", "Escreva o que aconteceu, o que você pensou ou o que não quer esquecer."],
      ["Organize", "Separe cada coisa no lugar certo e encontre de novo sem procurar por toda parte."],
      ["Relembre", "Volte ao seu histórico e veja como pessoas, datas e momentos se conectam."]
    ],
    archiveEyebrow: "O que cabe no OBUNTOS",
    archiveTitle: "Abra uma parte do seu arquivo.",
    archiveText: "Escolha uma área para entender como ela pode entrar na sua rotina.",
    archiveStamp: "8 áreas",
    archiveAria: "Áreas do OBUNTOS",
    moduleTabs: ["Diário", "Tarefas", "Calendário", "Metas", "Pessoas", "Biblioteca", "Finanças", "Estatísticas"],
    moduleCodes: ["DI", "TA", "CA", "ME", "PE", "BI", "FI", "ES"],
    privacyEyebrow: "Do seu jeito",
    privacyTitle: "Você escolhe como quer acessar o seu arquivo.",
    privacyText: "Dá para usar o OBUNTOS só neste computador. Se fizer sentido para você, também há conta e acesso por pendrive. Essas opções existem para ajudar, não para complicar o uso básico.",
    privacyNotes: ["Uso local", "Backups protegidos", "Conta opcional"],
    accessAria: "Formas de acesso",
    accessTabs: ["Neste computador", "Com uma conta", "Com pendrive"],
    installEyebrow: "Pronto para começar",
    installTitle: "Baixe, instale e faça dele o seu espaço.",
    installText: "O arquivo abaixo baixa e prepara o OBUNTOS no Windows. Depois é só abrir e criar o seu arquivo pessoal.",
    installSteps: [
      ["Baixe", "Salve o instalador no seu computador."],
      ["Instale", "Abra o arquivo e siga os passos na tela."],
      ["Comece", "Crie seu perfil e organize o que quiser guardar."]
    ],
    version: "Versão 0.9.3",
    downloadTitle: "OBUNTOS para Windows",
    downloadText: "Instalador para Windows x64 · 17,3 MB",
    downloadButton: "Baixar OBUNTOS",
    metaLabels: ["Sistema", "Versão", "Arquivo"],
    verification: "Detalhes de verificação",
    copy: "Copiar código",
    copied: "Código copiado",
    copyFallback: "Selecione o código para copiar",
    faqEyebrow: "Dúvidas rápidas",
    faqTitle: "Antes de baixar.",
    faq: [
      ["Preciso de internet para usar?", "Não para usar um perfil local. Internet só entra quando você usa recursos que dependem de conta ou sincronização."],
      ["Onde ficam as minhas coisas?", "O perfil local fica no seu computador. Se você optar por sincronização, o conteúdo enviado é protegido antes de sair do aplicativo."],
      ["Preciso criar uma conta?", "Não. Você pode começar e continuar usando o OBUNTOS com um perfil local. A conta é uma opção extra."],
      ["O que posso guardar nele?", "Diário, tarefas, calendário, metas, pessoas, biblioteca com anexos, finanças e estatísticas fazem parte do arquivo."],
      ["E o pendrive, para que serve?", "Ele pode ser usado como uma forma extra de acesso. Se você quiser, também pode configurar um modo que exige o pendrive para abrir o arquivo local."]
    ],
    footerVersion: "Versão 0.9.3 para Windows x64",
    footerTop: "Voltar ao início ↑"
  },
  en: {
    title: "OBUNTOS — Personal Archive",
    description: "OBUNTOS — your personal archive for Windows. Journal, tasks, calendar, goals, people, library, finances and statistics in one place.",
    skip: "Skip to content",
    brandAria: "OBUNTOS, home",
    brandSubtitle: "personal archive",
    navAria: "Main navigation",
    nav: ["Discover", "What's inside", "Your data", "Download"],
    headerDownload: "Download for Windows",
    menuOpen: "Open menu",
    menuClose: "Close menu",
    switchAria: "Mudar site para português",
    switchTitle: "Português",
    bannerLeft: "personal archive",
    bannerRight: "made for Windows",
    bannerAlt: "Stylized laboratory that is part of the OBUNTOS visual identity",
    bannerNote: "A space of your own, inside your computer.",
    signAria: "OBUNTOS, personal archive",
    signSubtitle: "personal archive",
    heroEyebrow: "A place for the things that are part of your life",
    heroTitle: "Everything that matters to you, in one place.",
    heroLead: "OBUNTOS brings together notes, tasks, dates, people, goals, files and finances without turning your life into a spreadsheet. It's a personal archive you can open, organize and return to whenever you want.",
    heroPrimary: "Download for Windows",
    heroSecondary: "See what's inside",
    releaseAria: "Version information",
    release: ["<b>0.9.3</b> current version", "<b>Windows x64</b>", "<b>17.3 MB</b> setup"],
    storyEyebrow: "Made to keep up with you",
    storyTitle: "Your day changes. Your archive comes with you.",
    storyText: "Some things are worth keeping: an idea, a date, a person, a goal, an expense, a photo or simply a thought from today. OBUNTOS keeps everything close without mixing it all together.",
    storyCards: [
      ["Write it down", "Keep what happened, what you thought or what you don't want to forget."],
      ["Organize", "Give each thing its place and find it again without searching everywhere."],
      ["Look back", "Return to your history and see how people, dates and moments connect."]
    ],
    archiveEyebrow: "What belongs in OBUNTOS",
    archiveTitle: "Open a part of your archive.",
    archiveText: "Choose an area to see how it can fit into your routine.",
    archiveStamp: "8 areas",
    archiveAria: "OBUNTOS areas",
    moduleTabs: ["Journal", "Tasks", "Calendar", "Goals", "People", "Library", "Finances", "Statistics"],
    moduleCodes: ["JR", "TS", "CA", "GO", "PE", "LI", "FI", "ST"],
    privacyEyebrow: "Your way",
    privacyTitle: "You choose how you want to access your archive.",
    privacyText: "You can use OBUNTOS only on this computer. If it makes sense for you, there are also account and USB drive options. They're there to help, not to make the basic experience more complicated.",
    privacyNotes: ["Local use", "Protected backups", "Optional account"],
    accessAria: "Ways to access",
    accessTabs: ["On this computer", "With an account", "With a USB drive"],
    installEyebrow: "Ready to begin",
    installTitle: "Download it, install it and make it your space.",
    installText: "The file below downloads and prepares OBUNTOS on Windows. Then just open it and create your personal archive.",
    installSteps: [
      ["Download", "Save the installer to your computer."],
      ["Install", "Open the file and follow the steps on screen."],
      ["Start", "Create your profile and organize whatever you want to keep."]
    ],
    version: "Version 0.9.3",
    downloadTitle: "OBUNTOS for Windows",
    downloadText: "Installer for Windows x64 · 17.3 MB",
    downloadButton: "Download OBUNTOS",
    metaLabels: ["System", "Version", "File"],
    verification: "Verification details",
    copy: "Copy code",
    copied: "Code copied",
    copyFallback: "Select the code to copy",
    faqEyebrow: "Quick questions",
    faqTitle: "Before you download.",
    faq: [
      ["Do I need internet to use it?", "Not for a local profile. Internet is only needed when you use features that depend on an account or synchronization."],
      ["Where is my stuff stored?", "A local profile stays on your computer. If you choose synchronization, uploaded content is protected before it leaves the app."],
      ["Do I need to create an account?", "No. You can start and keep using OBUNTOS with a local profile. An account is an optional extra."],
      ["What can I keep in it?", "Journal entries, tasks, calendar items, goals, people, a library with attachments, finances and statistics are all part of the archive."],
      ["What is the USB drive option for?", "It can be used as an extra way to access your archive. If you want, you can also require the USB drive to be connected before the local archive can be opened."]
    ],
    footerVersion: "Version 0.9.3 for Windows x64",
    footerTop: "Back to top ↑"
  }
};

const modules = {
  pt: {
    diario: { code: "DI / 01", kicker: "Para lembrar do seu dia", title: "Diário", text: "Guarde acontecimentos, ideias e memórias para voltar a eles depois, sem precisar espalhar tudo em vários lugares.", tags: ["Anotações", "Memórias", "Histórico"], symbol: "DI", accent: "#d97732", soft: "#f2d695" },
    tarefas: { code: "TA / 02", kicker: "Para não deixar passar", title: "Tarefas", text: "Anote o que precisa fazer, escolha o que vem primeiro e acompanhe as pequenas coisas do dia sem transformar a rotina em trabalho.", tags: ["Pendências", "Prioridades", "Rotina"], symbol: "TA", accent: "#c95f35", soft: "#efbc86" },
    calendario: { code: "CA / 03", kicker: "Para saber quando", title: "Calendário", text: "Reúna compromissos, datas importantes e lembretes para enxergar melhor o que vem pela frente.", tags: ["Agenda", "Datas", "Lembretes"], symbol: "CA", accent: "#d8a536", soft: "#f1dc9b" },
    metas: { code: "ME / 04", kicker: "Para acompanhar seus planos", title: "Metas", text: "Guarde objetivos, divida em etapas e acompanhe o caminho sem perder de vista por que aquilo importa para você.", tags: ["Objetivos", "Etapas", "Progresso"], symbol: "ME", accent: "#a95b37", soft: "#e9c4a3" },
    pessoas: { code: "PE / 05", kicker: "Para lembrar de quem importa", title: "Pessoas", text: "Crie registros de pessoas importantes, anote detalhes e preserve um histórico que faça sentido para a sua vida.", tags: ["Pessoas", "Relações", "Histórico"], symbol: "PE", accent: "#d07a3a", soft: "#f0cf9f" },
    biblioteca: { code: "BI / 06", kicker: "Para guardar o que você consulta", title: "Biblioteca", text: "Reúna referências e anexos locais, de documentos e imagens a arquivos compactados como ZIP, RAR e 7Z.", tags: ["Anexos", "Referências", "Arquivos"], symbol: "BI", accent: "#b86b38", soft: "#e7c7a5" },
    financas: { code: "FI / 07", kicker: "Para entender seu dinheiro", title: "Finanças", text: "Registre entradas e saídas e consulte seu próprio histórico financeiro no mesmo lugar em que você organiza o resto da vida.", tags: ["Movimentos", "Histórico", "Resumo"], symbol: "FI", accent: "#c88e32", soft: "#ead48e" },
    estatisticas: { code: "ES / 08", kicker: "Para enxergar o que mudou", title: "Estatísticas", text: "Veja seus registros por outro ângulo e perceba padrões e mudanças que podem passar despercebidos no dia a dia.", tags: ["Padrões", "Resumo", "Evolução"], symbol: "ES", accent: "#9f6038", soft: "#e2c8ad" }
  },
  en: {
    diario: { code: "JR / 01", kicker: "To remember your day", title: "Journal", text: "Keep moments, ideas and memories so you can return to them later without scattering everything across different places.", tags: ["Notes", "Memories", "History"], symbol: "JR", accent: "#d97732", soft: "#f2d695" },
    tarefas: { code: "TS / 02", kicker: "So things don't slip by", title: "Tasks", text: "Write down what you need to do, decide what comes first and keep up with everyday things without turning your routine into more work.", tags: ["To-do", "Priorities", "Routine"], symbol: "TS", accent: "#c95f35", soft: "#efbc86" },
    calendario: { code: "CA / 03", kicker: "To know when", title: "Calendar", text: "Keep appointments, important dates and reminders together so you can see what is coming next.", tags: ["Schedule", "Dates", "Reminders"], symbol: "CA", accent: "#d8a536", soft: "#f1dc9b" },
    metas: { code: "GO / 04", kicker: "To follow your plans", title: "Goals", text: "Keep your goals, break them into steps and follow the path without losing sight of why they matter to you.", tags: ["Goals", "Steps", "Progress"], symbol: "GO", accent: "#a95b37", soft: "#e9c4a3" },
    pessoas: { code: "PE / 05", kicker: "To remember who matters", title: "People", text: "Create records for important people, note the little details and keep a history that makes sense in your life.", tags: ["People", "Relationships", "History"], symbol: "PE", accent: "#d07a3a", soft: "#f0cf9f" },
    biblioteca: { code: "LI / 06", kicker: "To keep what you return to", title: "Library", text: "Keep references and local attachments together, from documents and images to compressed files such as ZIP, RAR and 7Z.", tags: ["Attachments", "References", "Files"], symbol: "LI", accent: "#b86b38", soft: "#e7c7a5" },
    financas: { code: "FI / 07", kicker: "To understand your money", title: "Finances", text: "Record money coming in and going out, and review your own financial history in the same place you organize the rest of your life.", tags: ["Activity", "History", "Summary"], symbol: "FI", accent: "#c88e32", soft: "#ead48e" },
    estatisticas: { code: "ST / 08", kicker: "To see what changed", title: "Statistics", text: "Look at your records from another angle and notice patterns and changes that can be easy to miss day to day.", tags: ["Patterns", "Summary", "Progress"], symbol: "ST", accent: "#9f6038", soft: "#e2c8ad" }
  }
};

const modes = {
  pt: {
    local: { number: "01", label: "O jeito mais simples", title: "Use só no seu computador", text: "Crie seu perfil e abra o arquivo normalmente neste PC, sem precisar de uma conta para começar.", points: ["Seu perfil fica neste computador", "Conta não é obrigatória", "Backups e snapshots são protegidos"] },
    cloud: { number: "02", label: "Quando quiser levar mais longe", title: "Conecte uma conta", text: "A conta adiciona identidade e sincronização opcional. Você decide se quer usar isso ou continuar só com o perfil local.", points: ["Identidade OBUNTOS", "Sincronização opcional", "Conteúdo protegido antes do envio"] },
    usb: { number: "03", label: "Uma chave física", title: "Use um pendrive", text: "Você pode autorizar um pendrive para facilitar o acesso ou, se preferir, exigir que ele esteja conectado para abrir o arquivo local.", points: ["Uso opcional", "Pode agilizar a entrada", "Também pode virar uma exigência de acesso"] }
  },
  en: {
    local: { number: "01", label: "The simplest way", title: "Use it only on your computer", text: "Create your profile and open the archive normally on this PC, without needing an account to get started.", points: ["Your profile stays on this computer", "An account is not required", "Backups and snapshots are protected"] },
    cloud: { number: "02", label: "When you want to take it further", title: "Connect an account", text: "An account adds identity and optional synchronization. You decide whether to use it or keep everything in your local profile.", points: ["OBUNTOS identity", "Optional synchronization", "Content is protected before upload"] },
    usb: { number: "03", label: "A physical key", title: "Use a USB drive", text: "You can authorize a USB drive to make access easier or, if you prefer, require it to be connected before the local archive opens.", points: ["Optional to use", "Can make access quicker", "Can also become an access requirement"] }
  }
};

const $ = (selector, root = document) => root.querySelector(selector);
const $$ = (selector, root = document) => [...root.querySelectorAll(selector)];
let currentLanguage = localStorage.getItem("obuntos-language") || (navigator.language.toLowerCase().startsWith("pt") ? "pt" : "en");
let currentModule = "diario";
let currentMode = "local";

function setText(selector, value, root = document) {
  const element = $(selector, root);
  if (element) element.textContent = value;
}

function setTexts(selector, values, root = document) {
  $$(selector, root).forEach((element, index) => {
    if (values[index] !== undefined) element.textContent = values[index];
  });
}

function setHtmls(selector, values, root = document) {
  $$(selector, root).forEach((element, index) => {
    if (values[index] !== undefined) element.innerHTML = values[index];
  });
}

function renderModule(key) {
  currentModule = key;
  const data = modules[currentLanguage][key];
  const panel = $("[data-module-panel]");
  if (!data || !panel) return;
  panel.classList.add("is-switching");
  setTimeout(() => {
    $("[data-module-code]").textContent = data.code;
    $("[data-module-kicker]").textContent = data.kicker;
    $("[data-module-title]").textContent = data.title;
    $("[data-module-text]").textContent = data.text;
    $("[data-module-symbol]").textContent = data.symbol;
    $("[data-module-tags]").innerHTML = data.tags.map(tag => `<span>${tag}</span>`).join("");
    panel.style.setProperty("--module-accent", data.accent);
    panel.style.setProperty("--module-soft", data.soft);
    $$("[data-module]").forEach(button => button.setAttribute("aria-selected", String(button.dataset.module === key)));
    panel.classList.remove("is-switching");
  }, 110);
}

function renderMode(key) {
  currentMode = key;
  const data = modes[currentLanguage][key];
  if (!data) return;
  $("[data-mode-number]").textContent = data.number;
  $("[data-mode-label]").textContent = data.label;
  $("[data-mode-title]").textContent = data.title;
  $("[data-mode-text]").textContent = data.text;
  $("[data-mode-points]").innerHTML = data.points.map(point => `<li>${point}</li>`).join("");
  $$("[data-mode]").forEach(button => button.setAttribute("aria-selected", String(button.dataset.mode === key)));
}

function updateMenuLabel() {
  const button = $("[data-menu-toggle]");
  const nav = $("[data-nav]");
  if (!button || !nav) return;
  const open = nav.classList.contains("is-open");
  button.setAttribute("aria-label", open ? content[currentLanguage].menuClose : content[currentLanguage].menuOpen);
}

function applyLanguage(language) {
  currentLanguage = language === "en" ? "en" : "pt";
  const copy = content[currentLanguage];
  document.documentElement.lang = currentLanguage === "pt" ? "pt-BR" : "en";
  document.title = copy.title;
  const description = $('meta[name="description"]');
  if (description) description.setAttribute("content", copy.description);
  localStorage.setItem("obuntos-language", currentLanguage);

  setText(".skip-link", copy.skip);
  const brand = $(".brand");
  if (brand) brand.setAttribute("aria-label", copy.brandAria);
  setText(".brand-copy small", copy.brandSubtitle);
  const nav = $(".main-nav");
  if (nav) nav.setAttribute("aria-label", copy.navAria);
  setTexts(".main-nav a", copy.nav);
  setText(".header-download", copy.headerDownload);

  const languageButton = $("[data-language-toggle]");
  if (languageButton) {
    languageButton.textContent = currentLanguage === "pt" ? "EN" : "PT";
    languageButton.setAttribute("aria-label", copy.switchAria);
    languageButton.title = copy.switchTitle;
  }
  updateMenuLabel();

  setText(".banner-side.left", copy.bannerLeft);
  setText(".banner-side.right", copy.bannerRight);
  const bannerImage = $(".banner-stage img");
  if (bannerImage) bannerImage.alt = copy.bannerAlt;
  setText(".banner-note", copy.bannerNote);
  const sign = $(".retro-sign");
  if (sign) sign.setAttribute("aria-label", copy.signAria);
  setText(".sign-subtitle", copy.signSubtitle);

  setText(".hero-copy .eyebrow", copy.heroEyebrow);
  setText(".hero-copy h1", copy.heroTitle);
  setText(".hero-lead", copy.heroLead);
  setTexts(".hero-actions a", [copy.heroPrimary, copy.heroSecondary]);
  const releaseStrip = $(".release-strip");
  if (releaseStrip) releaseStrip.setAttribute("aria-label", copy.releaseAria);
  setHtmls(".release-strip span", copy.release);

  setText(".story-copy .eyebrow", copy.storyEyebrow);
  setText(".story-copy h2", copy.storyTitle);
  const storyParagraphs = $$(".story-copy > p");
  if (storyParagraphs[1]) storyParagraphs[1].textContent = copy.storyText;
  $$(".story-card").forEach((card, index) => {
    const item = copy.storyCards[index];
    if (!item) return;
    setText("h3", item[0], card);
    setText("p", item[1], card);
  });

  setText(".section-heading .eyebrow", copy.archiveEyebrow);
  setText(".section-heading h2", copy.archiveTitle);
  const archiveParagraphs = $$(".section-heading > div > p");
  if (archiveParagraphs[1]) archiveParagraphs[1].textContent = copy.archiveText;
  setText(".section-stamp", copy.archiveStamp);
  const moduleTabs = $(".module-tabs");
  if (moduleTabs) moduleTabs.setAttribute("aria-label", copy.archiveAria);
  $$(".module-tabs button").forEach((button, index) => {
    const label = copy.moduleTabs[index];
    const code = copy.moduleCodes[index];
    if (code !== undefined && label !== undefined) button.innerHTML = `<span>${code}</span>${label}`;
  });

  setText(".privacy-copy .eyebrow", copy.privacyEyebrow);
  setText(".privacy-copy h2", copy.privacyTitle);
  const privacyParagraphs = $$(".privacy-copy > p");
  if (privacyParagraphs[1]) privacyParagraphs[1].textContent = copy.privacyText;
  setTexts(".privacy-notes span", copy.privacyNotes);
  const accessTabs = $(".access-tabs");
  if (accessTabs) accessTabs.setAttribute("aria-label", copy.accessAria);
  setTexts(".access-tabs button", copy.accessTabs);

  setText(".install-section .section-intro .eyebrow", copy.installEyebrow);
  setText(".install-section .section-intro h2", copy.installTitle);
  const installIntroParagraphs = $$(".install-section .section-intro > p");
  if (installIntroParagraphs[1]) installIntroParagraphs[1].textContent = copy.installText;
  $$(".install-steps article").forEach((step, index) => {
    const item = copy.installSteps[index];
    if (!item) return;
    setText("h3", item[0], step);
    setText("p", item[1], step);
  });

  setText(".download-brand small", copy.brandSubtitle);
  setText(".download-main .eyebrow", copy.version);
  setText(".download-main h2", copy.downloadTitle);
  const downloadParagraph = $(".download-main > p:not(.eyebrow)");
  if (downloadParagraph) downloadParagraph.textContent = copy.downloadText;
  setText(".download-main .button", copy.downloadButton);
  setTexts(".download-meta > div > span", copy.metaLabels);
  setText(".checksum summary", copy.verification);
  setText("[data-copy-hash]", copy.copy);

  const faqIntro = $(".faq-section .section-intro");
  if (faqIntro) {
    setText(".eyebrow", copy.faqEyebrow, faqIntro);
    setText("h2", copy.faqTitle, faqIntro);
  }
  $$(".faq-list details").forEach((details, index) => {
    const item = copy.faq[index];
    if (!item) return;
    setText("summary", item[0], details);
    setText("p", item[1], details);
  });

  setText(".footer-brand span:last-child", copy.brandSubtitle);
  setText(".site-footer p", copy.footerVersion);
  setText(".site-footer a", copy.footerTop);

  renderModule(currentModule);
  renderMode(currentMode);
}

function showToast(message) {
  const toast = $("[data-toast]");
  toast.textContent = message;
  toast.classList.add("is-visible");
  clearTimeout(showToast.timer);
  showToast.timer = setTimeout(() => toast.classList.remove("is-visible"), 1800);
}

function initHeader() {
  const header = $("[data-header]");
  const sync = () => header.classList.toggle("is-scrolled", scrollY > 14);
  sync();
  addEventListener("scroll", sync, { passive: true });
}

function initLanguage() {
  const actions = $(".header-actions");
  const download = $(".header-download");
  if (!actions || !download) return;
  const button = document.createElement("button");
  button.type = "button";
  button.className = "button secondary";
  button.dataset.languageToggle = "";
  button.style.minHeight = "40px";
  button.style.padding = "0 13px";
  button.style.fontSize = ".72rem";
  button.style.letterSpacing = ".06em";
  button.style.cursor = "pointer";
  actions.insertBefore(button, download);
  button.addEventListener("click", () => applyLanguage(currentLanguage === "pt" ? "en" : "pt"));
  applyLanguage(currentLanguage);
}

function initMenu() {
  const button = $("[data-menu-toggle]");
  const nav = $("[data-nav]");
  if (!button || !nav) return;
  const close = () => {
    nav.classList.remove("is-open");
    button.setAttribute("aria-expanded", "false");
    updateMenuLabel();
  };
  button.addEventListener("click", () => {
    const open = !nav.classList.contains("is-open");
    nav.classList.toggle("is-open", open);
    button.setAttribute("aria-expanded", String(open));
    updateMenuLabel();
  });
  $$("a", nav).forEach(link => link.addEventListener("click", close));
  addEventListener("resize", () => { if (innerWidth > 820) close(); });
}

function initTabs() {
  $$("[data-module]").forEach(button => button.addEventListener("click", () => renderModule(button.dataset.module)));
  $$("[data-mode]").forEach(button => button.addEventListener("click", () => renderMode(button.dataset.mode)));
  renderModule(currentModule);
  renderMode(currentMode);
}

function initFaq() {
  $$(".faq-list details").forEach(details => details.addEventListener("toggle", () => {
    if (!details.open) return;
    $$(".faq-list details").forEach(other => { if (other !== details) other.open = false; });
  }));
}

function initCopy() {
  const button = $("[data-copy-hash]");
  const hash = $("[data-hash]");
  if (!button || !hash) return;
  button.addEventListener("click", async () => {
    try {
      await navigator.clipboard.writeText(hash.textContent.trim());
      showToast(content[currentLanguage].copied);
    } catch {
      showToast(content[currentLanguage].copyFallback);
    }
  });
}

function initReveal() {
  document.documentElement.classList.add("reveal-ready");
  const items = $$("[data-reveal]");
  if (matchMedia("(prefers-reduced-motion: reduce)").matches || !("IntersectionObserver" in window)) {
    items.forEach(item => item.classList.add("is-visible"));
    return;
  }
  const observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (!entry.isIntersecting) return;
      entry.target.classList.add("is-visible");
      observer.unobserve(entry.target);
    });
  }, { threshold: .11 });
  items.forEach(item => observer.observe(item));
}

function initSignMotion() {
  const sign = $(".retro-sign");
  if (!sign || matchMedia("(prefers-reduced-motion: reduce)").matches) return;
  sign.addEventListener("pointermove", event => {
    const rect = sign.getBoundingClientRect();
    const x = (event.clientX - rect.left) / rect.width - .5;
    const y = (event.clientY - rect.top) / rect.height - .5;
    sign.style.transform = `rotate(${-0.4 + x * 0.55}deg) translate(${x * 2}px, ${y * 2}px)`;
  });
  sign.addEventListener("pointerleave", () => sign.style.transform = "rotate(-.4deg)");
}

initHeader();
initLanguage();
initMenu();
initTabs();
initFaq();
initCopy();
initReveal();
initSignMotion();
